# membership-card-verifier

Python port of `@membership-card-protocol/verifier`. Verifies `SignedMessageEnvelope` objects produced by the Card Protocol.

Under construction — see `plans/python-verifier-implementation-plan.md`.
