{
  "name": "membership-card-verifier",
  "private": false,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm --filter @membership-card-protocol/verifier build && pnpm -r typecheck"
  },
  "packageManager": "pnpm@9.6.0",
  "engines": {
    "node": ">=22",
    "pnpm": ">=9"
  }
}
