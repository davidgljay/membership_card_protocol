Added `test/version.test.ts` (4 unit tests for `extractProtocolVersion` covering valid, absent, unrecognized, and non-string cases) and `test/integration/versioning.test.ts` (3 integration tests covering normal v0.1 flow, missing version, and unknown version). Created `specs/versioning-test-vectors.json` with one `CardDocument` vector and one `SignedMessageEnvelope.payload` vector including expected RFC 8785 canonical JSON. Created `specs/protocol-versioning.md` documenting the version format, field placement, canonical ordering implications, v0.1 scope, rejection policy, and upgrade procedure. All 85 tests pass with zero regressions.
