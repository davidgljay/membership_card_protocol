Added `protocol_version: string` (required) to `CardDocument` and `SignedMessageEnvelope.payload`, and introduced `PROTOCOL_VERSION_0_1`, `KNOWN_PROTOCOL_VERSIONS`, and `ProtocolVersion` constants in `constants.ts`. All test fixtures were updated to include `protocol_version: "0.1"`, the Rust `sign_card_message.rs` usage comment was updated to document the field requirement, and `pnpm tsc --noEmit` and `pnpm vitest run` both exit 0 with no regressions.
