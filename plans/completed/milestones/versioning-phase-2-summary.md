Created `src/version.ts` with `extractProtocolVersion`, which throws typed `CardProtocolError` for missing or unrecognized versions. Updated `EnvelopeVerificationResult` and `CardVerificationResult` to carry `protocol_version: string`. Wired version extraction into `CardVerifier.verifyEnvelope` — unknown/missing versions now return a structured result with the appropriate error code at stage 1 rather than throwing. `verifyCard` always returns `protocol_version: "0.1"` (the only version that can reach that path). All new exports are available via `index.ts`. `pnpm tsc --noEmit` and `pnpm vitest run` both exit 0.
