# harnesses/rn/

jest + react-native-preset container for `sdk-providers-rn`, mirroring the
web harness's smoke test (no emulator — jest-only RN simulation per the
strategic plan's Open Questions).

Filled in during Phase 2 (`integration-testing-implementation-plan.md` §2.3).
