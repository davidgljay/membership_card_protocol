# harnesses/web/

Playwright container loading a minimal page that exercises
`sdk-providers-web` + `client-sdk`/`app-sdk` against the live stack.
Smoke test: create wallet → accept an offer → validate card.

Filled in during Phase 2 (`integration-testing-implementation-plan.md` §2.2).
