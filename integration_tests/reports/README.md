# reports/

Dated defect reports from each wave's first run against a clean stack.
Each report lists, per failure: spec reference, components involved,
observed vs. expected behavior, suspected cause, and a triage tag
(fix-now / defer / test-bug).

Populated starting Phase 3 Step 3.3 (Wave 1 run + report).
