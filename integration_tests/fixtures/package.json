{
  "name": "@membership-card-protocol/integration-fixtures",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "description": "Shared test key material, a permissive test policy, and a live-stack card-minting helper for integration_tests' suites and harnesses.",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "//": "file: references match the convention already used by press/app-sdk for these same two packages, both of which build dist/ ahead of time.",
  "dependencies": {
    "@membership-card-protocol/app-sdk": "file:../../app-sdk/packages/app-sdk",
    "@noble/hashes": "^1.7.2"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4"
  }
}
