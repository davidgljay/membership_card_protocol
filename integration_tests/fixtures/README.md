# fixtures/

Shared keys, signed cards, and test vectors used across integration suites,
reused where possible from existing component test suites rather than
duplicated:

- `contracts/test_params`
- `contracts/scripts/gen_test_vectors.rs` output
- `press/test`
- `relay/tests`
- `wallet-service/test`

Filled in during Phase 2 (`integration-testing-implementation-plan.md` §2.1).
