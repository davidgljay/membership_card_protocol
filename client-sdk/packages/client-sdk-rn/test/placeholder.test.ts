// Placeholder so `pnpm -r test` has something to run before Step 1.6 adds
// real RN provider implementations and their contract-suite test files.
describe('client-sdk-rn scaffold', () => {
  it('package loads', () => {
    expect(true).toBe(true);
  });
});
