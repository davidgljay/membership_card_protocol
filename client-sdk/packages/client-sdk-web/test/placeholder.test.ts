import { describe, it, expect } from 'vitest';

// Placeholder so `pnpm -r test` has something to run before Step 1.5 adds
// real web provider implementations and their contract-suite test files.
describe('client-sdk-web scaffold', () => {
  it('package loads', () => {
    expect(true).toBe(true);
  });
});
